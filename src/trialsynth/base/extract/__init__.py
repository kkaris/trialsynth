"""TrialSynth base extract package

This submodule requires the `boto3` package. Install it with:

    pip install boto3

or include it when installing the `trialsynth` package with:

    pip install trialsynth[llm]
"""
